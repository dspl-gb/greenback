# Copy to .env.local and fill in - never commit the real file.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only - service role has full DB access, bypasses RLS. Route Handlers only.
SUPABASE_SERVICE_ROLE_KEY=

# Wallet integrations (see TDD section 8.2) - fill in once certs/keys are provisioned
APPLE_PASS_TYPE_ID=
APPLE_TEAM_ID=
GOOGLE_WALLET_ISSUER_ID=

# Payout provider (TBD - see TDD section 10)
PAYOUT_PROVIDER_API_KEY=
